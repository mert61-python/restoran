# Statik site — derleme yok, kök klasör yayınlanır.
[build]
  publish = "."
  command = ""

# Yönetim panelinin kaydetme fonksiyonu (şifre + GitHub anahtarı burada, sunucuda)
[functions]
  directory = "netlify/functions"
